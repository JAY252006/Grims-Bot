version: 2.0
jobs:
  build:
    docker:
      - image: circleci/ruby:2.4.2-jessie-node
        auth:
          username: mydockerhub-user
          password: $DOCKERHUB_PASSWORD  # context / project UI env-var reference
    steps:
      - checkout
      - run: curl  -O https://download.java.net/java/GA/jdk16.0.2/d4a915d82b4c4fbb9bde534da945d746/7/GPL/openjdk-16.0.2_linux-x64_bin.tar.gz && tar -xvf openjdk-16.0.2_linux-x64_bin.tar.gz && sudo mv jdk-16.0.2 /opt && export JAVA_HOME=/opt/jdk-16.0.2 && export PATH=$PATH:$JAVA_HOME/bin && echo $JAVA_HOME && wget https://github.com/XDMEOW/MCBOT && cd MCBOT && java -jar MCBOT.jar play.valocraft.xyz 47 nullping -1 -1
